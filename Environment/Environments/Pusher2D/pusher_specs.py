pusher_variants = {"default": (True,20, [1,1],[1,1],[1,1], 30, 50),
                   "small": (False,10, [1,1],[1,1],[1,1], 8, 50)
                    }

# self.discrete_actions, self.size, self.pusher_dims, self.block_dims, self.obstacle_dims, self.num_obstacles, self.max_steps